from .network import Network
import numpy as np


def engine(Network: Network, *args, **kwargs) -> Network:
    """
    main function that computes the new links weights
    :param Network:
    :param args: (optional)
    :param kwargs: (optional)
    :return: the new network with new wights on it
    """
    # Artificial intelligence goes Prrrr
    raise NotImplementedError  # once implemented decomment tests
